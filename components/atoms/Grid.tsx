import { styled } from '../../styled-system/jsx';

/**
 * Grid nasce com display="grid".
 * Excelente para layouts de páginas ou galerias.
 */
export const Grid = styled('div', {
  base: {
    display: 'grid',
  },
});